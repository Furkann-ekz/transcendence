services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: transcendence_backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/usr/src/app
      # Bu satır, konteynerin kendi node_modules'ünü korur.
      - /usr/src/app/node_modules
    networks:
      - transcendence_network
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: transcendence_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/usr/src/app
      # Bu satır, konteynerin kendi node_modules'ünü korur.
      - /usr/src/app/node_modules
    depends_on:
      - backend
    networks:
      - transcendence_network

networks:
  transcendence_network:
    driver: bridge